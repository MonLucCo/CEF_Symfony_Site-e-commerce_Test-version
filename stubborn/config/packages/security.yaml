security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'

    # https://symfony.com/doc/current/security.html#loading-the-user-the-user-provider
    providers:
        # Provider existant (conservé)
        users_in_memory: { memory: null }

        # Provider personnalisé pour charger les utilisateurs depuis la base de données
        users_in_database:
            entity:
                class: App\Entity\User
                property: email

    firewalls:
        dev:
            # Ensure dev tools and static assets are always allowed
            pattern: ^/(_profiler|_wdt|assets|build)/
            security: false
        main:
            lazy: true
            provider: users_in_database             # Le provider utilisé pour l'authentification

            form_login:
                login_path: app_login               # Route du formulaire de connexion
                check_path: app_login               # Route de soumission du formulaire de connexion
                enable_csrf: true                   # Protection CSRF activée pour le formulaire de connexion
                default_target_path: app_dashboard  # Route de redirection après une connexion réussie

            logout:
                path: app_logout                    # Route de déconnexion
                target: app_home                    # Route de redirection après la déconnexion                
                invalidate_session: true            # Invalide la session lors de la déconnexion

            # Activate different ways to authenticate:
            # https://symfony.com/doc/current/security.html#the-firewall

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

    # Note: Only the *first* matching rule is applied
    access_control:
        - { path: ^/admin, roles: ROLE_ADMIN }
        # - { path: ^/profile, roles: ROLE_USER }

when@test:
    security:
        password_hashers:
            # Password hashers are resource-intensive by design to ensure security.
            # In tests, it's safe to reduce their cost to improve performance.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon

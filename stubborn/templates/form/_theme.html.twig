{% block form_label %}
	{% if label is not same as(false) %}
		<label {{ block('attributes') }} class="form-label">
			{{ label|trans({}, translation_domain) }}
			{% if required %}
				<span class="text-danger">*</span>
			{% endif %}
		</label>
	{% endif %}
{% endblock %}
